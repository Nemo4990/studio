/**
 * Core Philosophy: This ruleset enforces a strict user-ownership model for user-specific data,
 * while providing admins with access to top-level collections for platform-wide management.
 * This dual-structure ensures both security and query efficiency.
 *
 * Data Structure:
 * - User-Private Data: `/users/{userId}` contains user profiles. All personal transaction history
 *   (deposits, withdrawals, testimonials) is stored in subcollections (e.g., `/users/{userId}/deposits/{depositId}`).
 *   This ensures a user can only access their own information through simple, secure path-based rules.
 * - Admin-Viewable Data: Top-level collections like `/deposits`, `/withdrawals`, and `/submissions`
 *   mirror the user-created data. This allows admins to query all requests across the platform efficiently
 *   without needing expensive collection group scans. Users can write to these collections on creation, but cannot read them.
 *
 * Key Security Decisions:
 * - Dual-Write Model: The application client writes transaction data to two places: the user's private subcollection
 *   and the admin's top-level review collection. This separation is key to the security model.
 * - Strict Ownership on Subcollections: The `/users/{userId}` path is the cornerstone of user security.
 *   Rules like `isOwner(userId)` ensure only the authenticated user matching the path can access their own data.
 * - Admin-Only Top-Level Collections: The top-level `/deposits`, `/withdrawals`, and `/submissions`
 *   collections are locked down for admin use (read, update, delete). A special `create` rule allows users
 *   to initiate a request by writing into this collection, but they cannot view or edit it afterward.
 */
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // -------------------------------------------------------------------------
    // Helper Functions
    // -------------------------------------------------------------------------

    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    function isAdmin() {
      // In a real app, this would check a custom claim: request.auth.token.admin == true
      return isSignedIn() && request.auth.token.email == 'admin@taskverse.io';
    }

    // -------------------------------------------------------------------------
    // Collection Rules
    // -------------------------------------------------------------------------

    match /users/{userId} {
      // This `get` rule is critical. It unblocks queries on all subcollections for the owner.
      allow get: if isOwner(userId) || isAdmin();
      // Only admins can list all users.
      allow list: if isAdmin();
      // A user can create their own document on signup.
      allow create: if isOwner(userId);
      // A user can update their own profile, an admin can update any.
      allow update: if isOwner(userId) || isAdmin();
      // User documents cannot be deleted.
      allow delete: if false;

      // --- User-Private Subcollections ---
      
      match /deposits/{depositId} {
        // This is the simplest possible rule for a user's query.
        allow list: if isOwner(userId);
        
        // These are for single-document operations.
        allow get: if isOwner(userId) || isAdmin();
        allow create: if isOwner(userId);
        allow update: if isAdmin(); // Only admin can update status
        allow delete: if isOwner(userId);
      }

      match /withdrawals/{withdrawalId} {
        // Simplest rule for user query.
        allow list: if isOwner(userId);

        // Single-document operations.
        allow get: if isOwner(userId) || isAdmin();
        allow create: if isOwner(userId);
        allow update: if isAdmin(); // Only admin can update status
        allow delete: if isOwner(userId);
      }
      
      match /testimonials/{testimonialId} {
        allow read, write: if isOwner(userId);
      }
    }

    match /tasks/{taskId} {
      allow read: if true;
      allow write: if isAdmin();
    }

    // Top-level collection for admin review.
    // Users can create (submit) but cannot read the list. Admins have full control.
    match /submissions/{submissionId} {
      allow create: if isOwner(request.resource.data.userId);
      allow read, update, delete: if isAdmin();
    }
    
    // Top-level collection for admin review of deposits.
    match /deposits/{depositId} {
       allow create: if isOwner(request.resource.data.userId);
       allow read, update, delete: if isAdmin();
    }
    
    // Top-level collection for admin review of withdrawals.
    match /withdrawals/{withdrawalId} {
       allow create: if isOwner(request.resource.data.userId);
       allow read, update, delete: if isAdmin();
    }

    match /agents/{agentId} {
      allow read: if isSignedIn();
      allow write: if isAdmin();
    }

    match /wallets/{walletId} {
      allow read, write: if false;
    }
  }
}
